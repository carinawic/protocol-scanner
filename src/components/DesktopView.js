import PageLayout from './PageLayout';

function DesktopView() {
  return (
    <PageLayout
      header='Use a mobile device'
      subheader='Protocol scanner is not supported on desktop.'
    />
  );
}

export default DesktopView;
